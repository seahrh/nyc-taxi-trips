GET /total_trips                        v1.trip.tripcount.TripCountController.show(start, end)

GET /average_fare_heatmap               v1.trip.averagefareheatmap.AverageFareHeatmapController.show(date)

GET /average_speed_24hrs                v1.trip.averagespeed.AverageSpeedController.show(date)

->         /v1/posts               v1.post.PostRouter

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.at(path="/public", file)

